# 스코어보드

화면과 스코어보드 API 를 같은 서버가 내보냅니다. 기록은 볼륨 위 SQLite 파일
하나에 씁니다. 띄우는 방법은 [DEPLOY.md](DEPLOY.md) 를 보세요. 이 문서는 순위를
무엇으로 어떻게 세는지를 적습니다.

## 점수 규칙

```text
성공 점수 = 자모 수 * (6 - 성공한 시도 횟수)
실패 점수 = 0
```

걸린 시간은 점수와 별도로 저장하고 순위 화면에 표시합니다. 닉네임과 브라우저별 익명 ID만 사용하므로 로그인이나 본인 인증은 제공하지 않습니다.

## 동작 범위

순위는 두 축입니다.

| | 값 | 뜻 |
|---|---|---|
| 기간 | `action=daily` | 오늘 순위 |
| | `action=overall` | 전체 순위 |
| 방식 | `mode=total` | 누적 점수 |
| | `mode=time` | 타임어택 |
| | `mode=score` | 점수어택 |

- **누적 점수**: 사람별 점수 합계. 총점 → 적은 판수 → 빠른 최고 기록 순.
  승률 · 평균 시도 · 평균 시간을 함께 내려보냅니다. 평균 둘은 **이긴 판만** 셉니다 —
  진 판은 늘 5번을 다 쓰고 끝나 섞으면 시도 평균이 눌리고 풀이 속도도 나타내지
  못합니다. 승률을 나란히 두어 이긴 판만 센다는 사실이 가려지지 않게 합니다
- **타임어택**: 사람별 가장 빨리 푼 한 판. 빠른 순 → 같은 시간이면 높은 점수
- **점수어택**: 사람별 한 판 최고 점수. 높은 순 → 같은 점수면 빠른 쪽
- 타임어택과 점수어택은 **이긴 판만** 셉니다. 진 판은 시간도 점수도 견줄
  대상이 아니라서, 아직 한 번도 못 이긴 사람은 두 표에 나오지 않습니다
- 상위 100명까지 내려주고, 잘렸다면 전체 인원을 함께 알려 줍니다
- 응답에 `mode` 를 되돌려 줍니다. 화면이 요청한 것과 다르면 서버 배포가
  오래됐다는 뜻이고, 그 때는 엉뚱한 표를 그리지 않고 그렇다고 알려 줍니다
- 같은 브라우저가 **같은 단어**를 다시 등록하면 서버가 거부합니다. 날짜는
  보지 않으므로 한 단어는 한 번만 점수가 됩니다

### 날짜 기준

'오늘'은 **서버 시간대**(`TZ`, 기본 `Asia/Seoul`) 하나로 정합니다. 브라우저는 날짜를
보내지 않고, 등록 시각도 서버가 찍습니다. 해외에서 접속해도 모두 같은 하루를 봅니다.

기록을 날짜로 조회하려면 `?action=daily&date=2026-08-20` 처럼 직접 부를 수 있습니다.
게임 화면은 이 값을 쓰지 않습니다.

### 왜 합산인가

판마다 새 단어를 뽑고 몇 번이든 다시 풀 수 있는 게임이라, 같은 문제를 놓고
겨루는 순위를 만들 수 없습니다. 대신 '오늘 얼마나 많이 · 잘 풀었나'를 셉니다.

### 한 단어, 한 번

중복 키는 `(clientId, puzzleId)` 입니다. `puzzleId` 는 정답 단어에서 나오므로
한 번 등록한 단어는 언제 다시 풀어도 점수가 되지 않습니다. 유일 인덱스로
막으므로 두 요청이 겹쳐 들어와도 한 줄만 남습니다.

날짜를 키에 넣지 않습니다. 등록한 단어를 다시 푸는 길은 결과 화면의 링크를
스스로 다시 여는 것뿐인데 그건 답을 아는 채로 푸는 것이고, 무작위로 다시 뽑히는
일은 화면 쪽에서 미리 걸러 냅니다(`js/store.js` 의 `isScored`).

화면도 등록한 단어를 기억해 등록 버튼을 미리 잠급니다. 서버는 마지막 관문입니다.

### 응답 속도

순위는 부를 때마다 계산합니다. 밀리초라 캐시를 두지 않았습니다. 캐시가 버는
것보다 '방금 등록한 점수가 아직 안 보인다'는 값이 더 큽니다.

화면은 같은 주소를 60초 동안 다시 부르지 않고(`js/scoreboard.js`), 판을 시작할 때와
끝났을 때 미리 받아 둡니다. 서버가 빨라진 지금은 없어도 되지만, 왕복을 한 번이라도
줄이는 편이 손해는 아니라 그대로 둡니다. 점수를 등록하면 그 캐시는 버립니다.

### 익명 ID

브라우저별 익명 ID(`clientId`)는 집계 키로만 쓰고 순위 응답에는 싣지 않습니다.
`/export` 로 백업을 받을 때만 함께 나갑니다.

점수 조작을 막는 장치는 없습니다. 자랑용 순위표 수준의 신뢰 모델입니다.

## 옛 배포 — Apps Script + Google Sheets

2026-08-25 까지는 GitHub Pages 에 화면을 올리고, 별도 서버를 두지 않으려고
Google Sheets 와 Apps Script Web App 을 저장소로 썼습니다. Pages 를 내리면서
그때까지의 기록 96행(21명, 6일치)을 SQLite 로 옮겼고, 지금은 쓰지 않습니다.

`backend/Code.gs` 를 남겨 둡니다. 옮겨 온 기록이 어떤 규칙으로 쌓였는지 확인할
자리가 필요하고, 원본 시트도 그대로 있습니다.

물러난 이유는 속도입니다. 시트를 여는 데만 2~3초가 걸렸고, 그 대부분이 웹앱의
요청당 고정 비용(`/exec` → 302 → `googleusercontent`)이라 서버를 손봐도 줄지
않았습니다. 지금은 같은 요청이 0.1초입니다.

옮기면서 시트에 있던 중복 2건이 빠졌습니다. 같은 `(clientId, puzzleId)` 가 두 번
들어간 것들로, Apps Script 의 중복 검사가 놓친 자리입니다. '한 단어, 한 번' 규칙대로
먼저 등록된 쪽을 남겼습니다.
