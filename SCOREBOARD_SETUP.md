# 스코어보드 연결

WordQuiz는 GitHub Pages에서 실행되므로 별도 서버를 운영하지 않고 Google Sheets와 Google Apps Script Web App을 저장소로 사용합니다.

## 1. Google Sheet 만들기

빈 Google Sheet를 만들고 URL을 복사합니다. 시트의 첫 번째 탭 이름은 `results`로 두세요. 헤더는 Apps Script가 처음 실행될 때 자동으로 만듭니다.

## 2. Apps Script 배포

1. Google Sheet에서 `확장 프로그램 > Apps Script`를 엽니다.
2. `backend/Code.gs` 내용을 붙여넣습니다.
3. `배포 > 새 배포`를 선택합니다.
4. 유형을 `웹 앱`으로 선택합니다.
5. 실행 주체는 본인, 액세스 권한은 `모든 사용자`로 설정합니다.
6. 배포된 웹 앱 URL을 복사합니다.

## 3. WordQuiz에 URL 넣기

`js/scoreboard.js`의 `API_URL`에 웹 앱 URL을 넣습니다.

```js
var API_URL = 'https://script.google.com/macros/s/배포ID/exec';
```

그 다음 커밋하고 `main` 브랜치에 푸시하면 GitHub Pages에 반영됩니다.

## 점수 규칙

```text
성공 점수 = 자모 수 * (6 - 성공한 시도 횟수)
실패 점수 = 0
```

걸린 시간은 점수와 별도로 저장하고 순위 화면에 표시합니다. 닉네임과 브라우저별 익명 ID만 사용하므로 로그인이나 본인 인증은 제공하지 않습니다.

## 동작 범위

순위는 두 축입니다.

| | 값 | 뜻 |
|---|---|---|
| 기간 | `action=daily` | 오늘 순위 |
| | `action=overall` | 전체 순위 |
| 방식 | `mode=total` | 누적 점수 |
| | `mode=time` | 타임어택 |
| | `mode=score` | 점수어택 |

- **누적 점수**: 사람별 점수 합계. 총점 → 적은 판수 → 빠른 최고 기록 순
- **타임어택**: 사람별 가장 빨리 푼 한 판. 빠른 순 → 같은 시간이면 높은 점수
- **점수어택**: 사람별 한 판 최고 점수. 높은 순 → 같은 점수면 빠른 쪽
- 타임어택과 점수어택은 **이긴 판만** 셉니다. 진 판은 시간도 점수도 견줄
  대상이 아니라서, 아직 한 번도 못 이긴 사람은 두 표에 나오지 않습니다
- 상위 100명까지 내려주고, 잘렸다면 전체 인원을 함께 알려 줍니다
- 응답에 `mode` 를 되돌려 줍니다. 화면이 요청한 것과 다르면 스코어보드 배포가
  오래됐다는 뜻이고, 그 때는 엉뚱한 표를 그리지 않고 그렇다고 알려 줍니다
- 같은 브라우저가 **같은 단어**를 다시 등록하면 Apps Script가 거부합니다. 날짜는
  보지 않으므로 한 단어는 한 번만 점수가 됩니다

### 날짜 기준

'오늘'은 **Apps Script 프로젝트의 시간대** 하나로 정합니다. 브라우저는 날짜를 보내지
않고, 등록 시각도 서버가 찍습니다. 해외에서 접속해도 모두 같은 하루를 봅니다.
시간대는 Apps Script 편집기의 `프로젝트 설정 > 시간대`에서 바꿉니다.

기록을 날짜로 조회하려면 `?action=daily&date=2026-08-20` 처럼 직접 부를 수 있습니다.
게임 화면은 이 값을 쓰지 않습니다.

### 왜 합산인가

판마다 새 단어를 뽑고 몇 번이든 다시 풀 수 있는 게임이라, 같은 문제를 놓고
겨루는 순위를 만들 수 없습니다. 대신 '오늘 얼마나 많이 · 잘 풀었나'를 셉니다.

### 한 단어, 한 번

중복 키는 `(clientId, puzzleId)` 입니다. `puzzleId` 는 정답 단어에서 나오므로
한 번 등록한 단어는 언제 다시 풀어도 점수가 되지 않습니다.

날짜를 키에 넣지 않습니다. 등록한 단어를 다시 푸는 길은 결과 화면의 링크를
스스로 다시 여는 것뿐인데 그건 답을 아는 채로 푸는 것이고, 무작위로 다시 뽑히는
일은 화면 쪽에서 미리 걸러 냅니다(`js/store.js` 의 `isScored`).
`puzzleDate` 칸은 시트가 Date 로 바꿔 저장하는데, 스프레드시트 시간대와 스크립트
시간대가 다르면 하루가 밀려 중복 검사가 조용히 새는 문제도 있었습니다.

화면도 등록한 단어를 기억해 등록 버튼을 미리 잠급니다. 서버는 마지막 관문입니다.

### 응답 속도

시트를 여는 데만 2~3초가 걸립니다. 순위는 등록이 있을 때만 바뀌므로
계산 결과를 `CacheService` 에 5분간 담아 둡니다. 한 번 읽을 때 기간 2 x 방식 3
= 여섯 조합을 모두 만들어 두므로 탭을 바꿔도 시트를 다시 열지 않습니다.
점수가 등록되면 그 날짜의 캐시를 지워 새 기록이 바로 보입니다.

화면도 같은 주소를 60초 동안 다시 부르지 않습니다(`js/scoreboard.js`).
점수를 등록하면 그 캐시도 버립니다.

### 익명 ID

브라우저별 익명 ID(`clientId`)는 집계 키로만 쓰고 순위 응답에는 싣지 않습니다.

Google Apps Script의 공개 Web App 특성상 점수 조작을 완전히 막을 수는 없습니다. 자랑용 순위표 수준의 신뢰 모델입니다.
