# WordQuiz 스타일 가이드

`pms` 프로젝트의 `STYLE_GUIDE.md` 와 같은 구성으로, 앱 공통 토큰은 이름까지 맞췄습니다.
게임에만 있는 개념(채점 표식)은 따로 둡니다.

검사는 사람 눈이 아니라 스크립트가 합니다.

```bash
python tools/check_css.py
```

---

## 1. 색상 시스템

테마는 `<html data-theme="dark|light">` 로 정해집니다. `js/theme.js` 가 `<head>` 에서
첫 페인트 전에 찍어 주므로, CSS 는 `prefers-color-scheme` 을 보지 않고 라이트
팔레트를 한 번만 적습니다.

### 앱 공통 (pms 와 이름 공유)

| 토큰 | 라이트 | 다크 | 용도 |
|------|--------|------|------|
| `--app-bg` | `#e8e8ec` | `#0d0d0f` | 카드 바깥 바탕 |
| `--app-card-bg` | `#ffffff` | `#1c1c1e` | 앱 카드 |
| `--app-text` | `#1a1a1c` | `#f2f2f2` | 본문 글자 |
| `--app-text-body` | `#3a3a3e` | `#d0d0d4` | 시트 본문 |
| `--app-text-muted` | `#5f5f64` | `#9a9aa0` | 보조 글자, 아이콘 |
| `--app-text-faint` | `#6b6b70` | `#8e8e93` | 가장 작은 주석 |
| `--app-border` | `#d3d6da` | `#3a3a3c` | 테두리, 자판 키 배경 |
| `--app-hover-bg` | `#e0e0e4` | `#2c2c2e` | 눌린 상태, 제출 버튼 |
| `--app-sunken-bg` | `#f4f4f6` | `#121214` | 입력칸, 순위 줄 |

### 채점 표식 (WordQuiz 전용)

| 토큰 | 라이트 | 다크 | 뜻 |
|------|--------|------|-----|
| `--ok` | `#42803d` | `#7ad17f` | 🟩 자리까지 맞음 |
| `--warn` | `#87741f` | `#ffd84a` | 🟨 있지만 다른 자리 |
| `--absent` | `#6b6f71` | `#d3d6da` | ⬜ 없음 |
| `--on-fill` | `#ffffff` | `#16161a` | 색이 칠해진 칸 **위의 글자색** |

`--ok` 는 강조색을 겸합니다(주요 버튼, 입력 포커스, 성공 문구).

### 그 밖

| 토큰 | 라이트 | 다크 | 용도 |
|------|--------|------|------|
| `--state-danger` | `#c0392b` | `#ff8a80` | 오류 문구 |
| `--tile-edge` | `#878a8c` | `#5a5a5e` | 입력 중인 타일 테두리 |
| `--scrim` | `rgba(0,0,0,.35)` | `rgba(0,0,0,.6)` | 시트 뒤 어둡게 |

---

## 2. `--on-fill` 이 왜 따로 있나

다크에서는 표식이 **밝고**, 라이트에서는 **짙습니다**. 그래서 그 위에 얹는 글자색이
테마마다 뒤집힙니다. 표식 위 글자에는 `--app-text` 가 아니라 반드시 `--on-fill` 을 씁니다.

```css
.tile.ok  { background: var(--ok);     color: var(--on-fill); }
.tile.off { background: var(--absent); color: var(--on-fill); }
```

`--app-text` 로 칠한 배경(선택된 칩, 토스트) 위에도 같은 이유로 `--on-fill` 을 씁니다.

---

## 3. 명도 대비

타일 글자는 `calc(var(--tile) * 0.46)` 이라 작은 화면에서 11px 까지 내려갑니다.
큰 글자 예외를 못 쓰므로 **모든 글자/배경 조합이 WCAG AA(4.5:1) 이상** 이어야 합니다.

라이트 팔레트가 워들 기본색(`#6aaa64` / `#c9b458`)보다 짙은 이유가 이것입니다.
그 색에 흰 글자를 얹으면 2.78 · 2.07 로 기준에 크게 못 미칩니다.

`python tools/check_css.py` 가 13개 조합을 전부 계산합니다.

---

## 4. 적용 원칙

1. **색은 토큰으로만** — `:root` 밖에서 `#`, `rgb()` 를 쓰지 않습니다.
2. **양쪽 테마를 함께** — 토큰을 새로 만들면 라이트 값도 같이 정합니다.
3. **대비를 먼저 확인** — 색을 바꾸면 `check_css.py` 를 돌립니다. 새 조합을 만들었으면
   `PAIRS` 에 한 줄 추가합니다.
4. **인라인 스타일 최소화** — js 가 만드는 HTML 에도 색을 박지 않고 클래스를 씁니다.
5. **주소창 색 동기화** — `js/theme.js` 의 `BAR` 는 `--app-bg` 와 같아야 합니다(검사됨).

---

## 5. 파일 구조

```
css/style.css       토큰 정의(:root, :root[data-theme="light"]) 와 전체 스타일
js/theme.js         data-theme 결정 · 저장 · 주소창 색 (head 에서 실행)
js/ui-*.js          화면 조각. 색은 클래스로만 지정
tools/check_css.py  위 원칙 검사
```
